import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Cell = () => {
  return (
    <View>
      <Text>Cell</Text>
    </View>
  )
}

export default Cell

const styles = StyleSheet.create({})